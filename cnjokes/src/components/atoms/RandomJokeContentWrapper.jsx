import React from 'react';

import '../../app.css';

const RandomJokeContentWrapper = ({ children }) => (
  <div className="random-joke-content">{children}</div>
);

export default RandomJokeContentWrapper;
