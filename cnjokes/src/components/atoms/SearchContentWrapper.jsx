import React from 'react';

import '../../app.css';

const SearchContentWrapper = ({ children }) => (
  <div className="search-jokes-content">{children}</div>
);

export default SearchContentWrapper;
